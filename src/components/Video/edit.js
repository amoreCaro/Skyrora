// import { TextControl } from '@wordpress/components';
import DefaultSection from "../DefaultSection/DefaultSection";

export const Video = ({ value, index, setAttributes }) => {
    const styles = {

    };
    return (
        <>
        </>
    );
};